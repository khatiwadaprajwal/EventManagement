# ------------------------------------
# 1. Dependencies & Logs (Too heavy/Variable)
# ------------------------------------
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
package-lock.json # OPTIONAL: Usually we COMMIT this, but if you have conflicts often, check with team. (Standard practice: KEEP it, do not ignore)

# ------------------------------------
# 2. Environment Variables (SECURITY RISK 🛑)
# ------------------------------------
# NEVER commit these. They contain your Google Client Secret & DB Passwords.
.env
.env.local
.env.development
.env.test
.env.production

# ------------------------------------
# 3. Build Artifacts (TypeScript -> JavaScript)
# ------------------------------------
# These are generated by running 'npm run build'. We don't commit generated code.
dist/
build/

# ------------------------------------
# 4. IDE / Editor Settings (Personal Preferences)
# ------------------------------------
.vscode/
.idea/
*.swp
*.sublime-workspace
.DS_Store

# ------------------------------------
# 5. Testing & Coverage
# ------------------------------------
coverage/

# ------------------------------------
# 6. Docker & Database Volumes
# ------------------------------------
# If you mapped your DB data to a local folder in docker-compose
postgres_data/
pgdata/
mongo_data/

# ------------------------------------
# 7. Prisma specific
# ------------------------------------
# We commit the schema and migrations, but ignore the generated SQLite file (if used)
# and internal generated clients if they are outputted to custom locations.
*.db
*.sqlite